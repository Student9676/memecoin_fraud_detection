import torch

# Replace with the path to your .pt file
file_path = '/Users/drew/Desktop/CS/CS 485/memecoin_fraud_detection/data/graphs/training_graphs/r_pwease_heterograph.pt'

# Load the .pt file
try:
    data = torch.load(file_path)
    print(f"Data loaded successfully from {file_path}")
    
    # If the loaded data is a tensor, print its shape and values
    if isinstance(data, torch.Tensor):
        print(f"Tensor shape: {data.shape}")
        print("Tensor values:")
        print(data)
    
    # If the loaded data is a dictionary (like a model or heterograph), print its keys
    elif isinstance(data, dict):
        print("Data contains the following keys:")
        print(data.keys())  # This will give you the keys in the dictionary (e.g., node types, edge types, etc.)
    
    else:
        print("The data is of an unknown type:", type(data))
        print(data)  # Print the data to inspect further
    
except Exception as e:
    print(f"Error loading file: {e}")